---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: HTTPRoute
metadata:
  name: plex
  namespace: media
spec:
  parentRefs:
    - name: media
  hostnames:
    - plex.192.168.86.2.nip.io
  rules:
    - matches:
      backendRefs:
        - name: plex
          port: 32400
