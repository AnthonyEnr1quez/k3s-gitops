---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: HTTPRoute
metadata:
  name: minio
  namespace: temp
spec:
  hostnames:
    - temp-minio.ant-podz.xyz
  parentRefs:
    - name: traefik-production
      namespace: networking
  rules:
    - backendRefs:
        - name: minio
          port: 9001
