---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: selfsigned
  namespace: networking
spec:
  secretName: selfsigned
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - "example.com"
    - "*.example.com"
