---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: velero
  namespace: velero
spec:
  chart:
    spec:
      chart: velero
      version: 3.1.6
      sourceRef:
        kind: HelmRepository
        name: vmware-tanzu
        namespace: flux-system
  interval: 1h
  maxHistory: 3
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    image:
      repository: gcr.io/velero-gcp/velero
      tag: v1.11.0@sha256:962935c3a39fa30a524e20a4a57ef389119571e987433714d60a745983085df2
    nodeSelector:
      kubernetes.io/hostname: gulsun
    tolerations:
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule

    backupsEnabled: true
    snapshotsEnabled: false
    upgradeCRDs: true
    cleanUpCRDs: true

    deployNodeAgent: true
    nodeAgent:
      tolerations:
        - key: plex
          operator: Exists
          effect: NoSchedule

    credentials:
      existingSecret: s3-provider
    configuration:
      extraEnvVars:
        TZ: America/Chicago
      provider: aws
      backupStorageLocation:
        name: default

    initContainers:
      - name: velero-plugin-for-aws
        image: gcr.io/velero-gcp/velero-plugin-for-aws:v1.6.1@sha256:c5b6a16d3ab84bcd55fe779c4b6f8c91f0d926c387ea690c5fa5803b4cd3d5e7
        imagePullPolicy: IfNotPresent
        volumeMounts:
          - mountPath: /target
            name: plugins

    schedules:
      daily-backup:
        enabled: true
        schedule:  0 4 * * *
        template:
          includedNamespaces:
            - download
            - home
            - media
          ttl: 360h
  valuesFrom:
    - targetPath: configuration.backupStorageLocation.bucket
      kind: Secret
      name: s3-provider
      valuesKey: BUCKET_NAME
    - targetPath: configuration.backupStorageLocation.config.s3Url
      kind: Secret
      name: s3-provider
      valuesKey: S3_URL
    - targetPath: configuration.backupStorageLocation.config.region
      kind: Secret
      name: s3-provider
      valuesKey: S3_REGION
