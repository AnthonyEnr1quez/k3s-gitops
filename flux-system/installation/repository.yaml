---
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: flux-installation
  namespace: flux-system
spec:
  interval: 30m
  url: https://github.com/fluxcd/flux2
  ref:
    # todo check this still updates without renovate comment
    tag: "v0.38.2"
  ignore: |
    # exclude all
    /*
    # path to manifests
    !/manifests
