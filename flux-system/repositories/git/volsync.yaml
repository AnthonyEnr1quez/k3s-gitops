apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: volsync
  namespace: flux-system
spec:
  interval: 1h
  url: https://github.com/AnthonyEnr1quez/volsync
  ref:
    branch: main
  ignore: |-
    # exclude all
    /*
    # include charts directory
    !/helm/
